#include <bits/stdc++.h>
#define ll long long int
using namespace std;

int main()
{
    ll n, x, y;
    cin>>n>>x>>y;
    if(n >= (2*x) && n >= (2*y)) cout<<"YES"<<endl;
    else cout<<"NO"<<endl;
    return 0;
}
