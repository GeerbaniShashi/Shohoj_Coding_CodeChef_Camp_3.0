#include <bits/stdc++.h>
#define ll long long int
using namespace std;

int main()
{
    ll x, y;
    cin>>x>>y;
    if(x > y) cout<<"FREEKICK"<<endl;
    else if(x < y) cout<<"PENALTY"<<endl;
    return 0;
}
